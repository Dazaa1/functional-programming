# Exercice 5 notes:
## Memoization:
- At it's core memoization is just caching storing a copy of a computation so we don't have to compute it again in the future.

```python
    def add(x, y):
        return x + y
```