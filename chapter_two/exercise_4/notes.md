# Exercise 5 notes:
``map``, ``filter``, ``reduce``, are three commonly used ``Higher-order functions`` in FP.

- map method takes a function and an ``iterable``, and returns and iterator that applies the function to every item, yielding the results.